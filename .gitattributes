# Normalise to LF in the repository. Without this, Windows checkouts get CRLF,
# which would leak into files this tool ships or reads at runtime.
* text=auto eol=lf

*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.whl binary
